# Bem vindo

Versao utilizada do Python 3.11
Instalar o framework Selenium atraves do comando no terminal "pip install selenium"
Instalar o framework Behave "pip install behave"
Instalar o web driver manager "pip install webdriver-manager"
